---
name: icf-ranking-siconfi
description: "Ranking Siconfi / ICF (nota A–E) como alvo externo do Gênesis; metodologia das 185 verificações e gap-list para um \"medidor de ICF\" (keystone = emitir MSC)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 50efd566-11fe-4e56-8e0b-efaf7198ed26
---

# ICF / Ranking Siconfi como alvo do Gênesis

> ⚠️ **ESTADO (2026-07-11) — ler antes de retomar.** **Dim I do ICF ENTREGUE:** validador `src/services/validador-msc.ts` (fn pura `validarEstruturaMsc` + `ValidadorMscService`, consome a MSC read-only) + endpoint `GET /memoriais/msc-validacao`, contrato **memoriais-lrf 1.16.0**. **14/18 checks ativos** — PRs **#231** (backbone Dim I) + **#233** (atributo-F + despesa) + **#235** (receita) mergeados. Os 4 stubs finais (poder/órgão, CO, AI, encerramento) dependem do EMISSOR, não do validador. Emissor da MSC: #225 backbone, #228 conta-corrente, #230 atributo-F/natureza-despesa/subfunção — TODOS mergeados em master; **NÃO tocar `matriz-saldos-contabeis.ts`** (frente do emissor/outra sessão). **ETAPA 3 (Dim IV) EM CURSO (sessão eabf10dd, 2026-07-11):** `scripts/backfill_contabil_execucao.ts` construído (worktree `genesis-wt-icf-dim4`, off `origin/master`; tsc limpo). **DRY-RUN VERDE** sobre a Prefeitura de Maringá 2026: 2.033 arrecadações + 22.756 movimentos de empenho **100% resolvidos pelo motor (0 bloqueados)**; **partida dobrada Δ0,00**; **reconciliação orçamentária Δ0,00 ao centavo** × execução capturada (= origem RREO/RGF): receita realizada 1.687.151.162,07 / empenhada 1.742.101.568,79 / liquidada 1.217.555.922,96 / paga 1.203.508.985,15. ⚠️ Descoberta importante: a nota "0 empenhos" do board estava ERRADA — a execução da despesa ESTÁ no dev (22.756 movimentos). ✅ **`--apply` APLICADO no dev (OK do Marco) — PR #236 MERGEADO** (`c2a9fbb`, CI verde; `scripts/backfill_contabil_execucao.ts` em master). Razão da execução populado: **53.421 lançamentos** (ARREC 4.532/EMPENHO 15.436/LIQ 18.337/PAG 15.116), partida dobrada global Δ0, reconciliação a nível de item ao centavo. **A MSC AGORA CARREGA EXECUÇÃO com conta-corrente** (6.167 linhas de execução no mês-pico, receita quebrada por natureza) e o **validador Dim I exercita execução real** (selo 10/14; ✗ são achados legítimos já documentados — `RECEITA_SEM_FONTE` gap do motor da receita, orçamentária/ativo invertida e atributo-F sem fonte da abertura — NÃO regressões). **Descoberta:** a nota "0 empenhos" do board estava ERRADA (22.756 movimentos no dev). Idempotente/reversível por origemTipo+origemId (ou `criadoPorId='BACKFILL_EXEC'`). ✅ **ETAPA 3b: `RECEITA_SEM_FONTE` FECHADO — PR #238 MERGEADO** (`4fba790`): `motor-eventos-receita.ts` carimba `fonteCodigo` em toda perna (E100 leva natureza+fonte, paralelo ao #226). Dev re-carimbado por UPDATE de cc (OK do Marco; sem impacto em saldo). **Validador Dim I: RECEITA_SEM_FONTE ✓ OK, selo Maringá 11/14.** ✗ restantes (3, todos estruturais legítimos): ATIVO_INVERTIDO (créditos a receber 1.1.2), ORCAMENTARIA_INVERTIDA (6.2.x natureza no modelo), ATRIBUTO_F_SEM_FONTE (disponibilidade abertura 1.1.1.1.1.01). **PRÓXIMO: cruzamentos Dim IV FORMAIS MSC↔RREO↔RGF↔DCA** (dependem do catálogo STN `descricao_ranking.csv` re-baixado) e/ou fechar as 3 ✗ estruturais. Regra durável: [[padroes-do-estado-canonicos]]. Ver item 3 abaixo + board [[coordenacao-sessoes]].

Disparado pela **Portaria STN/MF nº 1.833, de 24/06/2026** (DOU) — só DIVULGA o Ranking + o "IV Prêmio Qualidade da Informação Contábil e Fiscal" (dados de 2025); NÃO é norma técnica (base: Portaria STN/MF 807/2023). Ranking em https://ranking-municipios.tesouro.gov.br/. Maringá cai na categoria "municípios >100 mil hab.".

## Metodologia do ICF (Indicador da Qualidade da Informação Contábil e Fiscal)
- Nota = **% de acertos** em verificações automáticas sobre o que o ente ENVIOU ao Siconfi. **A** = >95%, **E** = <65% (5 faixas). Selo = nota A ("Aicf").
- Fontes: **DCA, RREO (6ºB), RGF (3ºQ/2ºS), MSC (Matriz de Saldos, mensal + dezembro)**. Corte 2026 = 10/05/2026.
- **185 verificações ativas em 2025**, 4 dimensões: I-Gestão (39), II-Contábil (74), III-Fiscal (44), IV-Contábil×Fiscal (28). Catálogo baixado de `descricao_ranking.csv` + `verificacoes_aplicabilidade.csv`; cópia legível ficou só no scratchpad (`catalogo_verificacoes_2026.md`) e **se perdeu no desligamento** — rebaixar do servidor do ranking quando for construir o validador estrutural (Etapa 4).

## De/para com o Gênesis (gap-list)
- **Guardião** (`src/services/memorial-guardiao.ts`, 9 indicadores) NÃO é o análogo — mede LIMITES da LRF (ortogonal ao ICF).
- Motor a reaproveitar = **Selo de Consistência** (`src/services/consistencia.ts`, V1–V8): mesmo padrão `Verificacao[]→selo`, envelope versionado `memoriais-lrf`. Hoje valida consistência INTERNA do razão, não os artefatos Siconfi.
- Gênesis TEM razão completo (`saldo-contabil.ts`/`razao-contabil.ts`/VPA-VPD/abertura/PCASP) e **RREO/RGF anexos VIVOS**, mas **NÃO emite MSC nem DCA** ← isso é a chave.
- **Vantagem única**: razão único → se emitir DCA+MSC do MESMO razão que gera RREO/RGF, os **28 cruzamentos da Dim IV passam por construção** (é onde os concorrentes perdem nota A). Teto honesto de auto-checagem ≈ 164/185; as 21 de entrega/prazo/retificação dependem do ATO de transmitir (fora de escopo de registro).

## Ordem de construção proposta
1. **Emissor de MSC** (keystone) — ✅ backbone MERGEADO (#225); a quebra por conta-corrente é a **fase 2, com OUTRA SESSÃO** (não é desta frente).
2. Validador estrutural da MSC → 18 checks da Dim I — ✅ **PR #231 ABERTO (sessão 50efd566, 2026-07-10)**: `src/services/validador-msc.ts` (fn pura `validarEstruturaMsc(linhas, {encerramento?})` + `ValidadorMscService`, reusa `Verificacao`/selo do `consistencia.ts`, consome `LinhaMsc` read-only) + endpoint `GET /memoriais/msc-validacao` (contrato **1.15.0**). **8 checks ATIVOS** (saldo invertido por natureza — ativo/passivo/PL/VPD/VPA/orçamentária separados; classes 1-8; fonte dígito-9 via `contaCorrente.fonte`) e **10 STUBS `NAO_APLICAVEL`** com id STN. Consolidou o validador que estava DUPLICADO com o `msc-validador.ts` da sessão do worktree `genesis-wt-msc-val` (fundiu passivo/PL separado + caminho de encerramento). Landado em worktree limpo off master (`feat/icf-validador-dim1`), 42 testes verdes, tsc limpo. **Lição:** `git worktree list` faz parte de "ler o board" em árvore multi-worktree (a duplicação teria sido evitada). ✅ **FOLLOW-UP #233 MERGEADO (2026-07-11, `dd0b74b`)**: ativou +4 checks que o #230 destravou — **atributo-F sem fonte** (via `LinhaMsc.superavitFinanceiro === 'FINANCEIRO'`) + **despesa sem natureza/função/fonte** (linha de despesa marcada por `contaCorrente.dotacaoId != null`, robusto sem chutar prefixo PCASP). Agora **12 checks ATIVOS / 6 stubs**. Validado sobre MSC REAL de Maringá: Prefeitura selo 10/12 (achados legítimos: 1 orçamentária invertida `6.2.1.2.0` + disponibilidade abertura `1.1.1.1.1.01` sem fonte). **Enum `superavitFinanceiro` = FINANCEIRO/PATRIMONIAL/MISTA/OUTROS_CONTROLES** (bruto 'F'→'FINANCEIRO' via `mapearSuperavitFinanceiro`). **Stubs restantes (6):** receita sem natureza/fonte (D1_00029/00030 — falta distinguir linha de receita por prefixo PCASP), poder/órgão (D1_00019, fase 2b não emitida), CO (D1_00041-43), AI (D1_00044), encerramento (D1_00036 — falta MSC de encerramento). Setup p/ rodar a MSC via script: `PrismaClient({ adapter: new PrismaPg(pool) })` (Prisma 7.7 exige adapter). ✅ **#235 MERGEADO (2026-07-11, `1f79b2a`)**: fechou os 2 stubs de RECEITA — **D1_00030 receita sem natureza** + **D1_00029 receita sem fonte**, identificando a linha de receita pelo **prefixo PCASP `6.2.1`** ("Execução da Receita": a realizar 6.2.1.1, realizada 6.2.1.2, (-) deduções 6.2.1.3; = "receita orçamentária e deduções" do catálogo, descrições conferidas no banco). Agora **14 checks ATIVOS / 4 stubs**. Maringá: receita-sem-natureza OK; receita-sem-fonte ✗ 2/2 (o motor da receita NÃO resolve fonte na cc — gap sistemático do emissor, diferente da despesa que resolve da dotação). **Stubs finais (4):** poder/órgão (D1_00019), CO (D1_00041-43), AI (D1_00044), encerramento (D1_00036) — TODOS dependem do EMISSOR produzir mais dimensões (poder/órgão, CO, AI) ou a MSC de encerramento; não são do validador. Frente Dim I ~esgotada do lado do validador. 3. Cruzamentos Dim IV (28) — 🟡 **DESTRAVÁVEL via BACKFILL do motor (investigado + CORRIGIDO 2026-07-11).** O razão contábil da Maringá está vazio de execução porque a captura importa DIRETO no orçamentário (`importar_arrecadacao_maringa_2026.ts`/`importar_execucao_entidades_2026.ts` criam Arrecadacao/MovimentoEmpenho + CAP-*, materializam valorArrecadado/valorEmpenhado) **pulando os Services que chamam o motor** ("não é escrituração, é captura p/ painéis"). **CORREÇÃO do meu diagnóstico anterior (estava errado):** a Dim IV NÃO precisa do de/para completo. O motor dispara os eventos ORÇAMENTÁRIOS **SEMPRE** (receita 100/200, despesa 600/601/700/701/800/801 — TODOS carregados no modelo PARANÁ); o de/para (`ParametroReceita/Despesa`, esparso: 22%/7%) só gate o PATRIMONIAL (300 VPA / 702 VPD / 802) = Dim II, não IV. Confirmado em `motor-eventos-receita.ts:121-139` ("orçamentário + DDR sempre; patrimonial só se parametrizado") e `motor-eventos-despesa.ts:107,134`. ⇒ **Dim IV = rodar um BACKFILL do motor sobre a execução já capturada** (iterar Arrecadacao + MovimentoEmpenho → resolver/resolverEmpenho/Liquidacao/Pagamento → LancamentosService.criar; idempotente por origem) → popula 6.2.1.2 (receita realizada) e 6.2.2.x (despesa) → a MSC passa a carregar a execução → cruzamentos batem por construção (mesma origem do RREO/RGF). Backfill contábil NÃO existe ainda (só `backfill_movimentos_empenho.ts`, que é orçamentário). Caveat: MUTA o dev (milhões de lançamentos) — precisa OK do Marco + validação. De/para completo (patrimonial/Dim II + DCA) é frente separada, e vem do PADRÃO DO ESTADO (ver [[padroes-do-estado-canonicos]]), não à mão. **Próximo: propor o backfill.** 4. Emissor de DCA → Dim II (74). 5. Envolver RREO/RGF (44) como verificações pontuadas. 6. Score/Selo ICF (nota A–E) no envelope versionado, consumível pelo Oxy.
Fases 1–3 = pré-ICF real (~46 checks "perde-ponto") sem depender de DCA.

### 3 decisões que ficaram abertas no plano (default sugerido)
1. **Escopo**: MSC por-entidade primeiro (default) vs. já consolidada por ente.
2. **Saldo inicial patrimonial por fonte**: usar saldos de abertura 2026 já cravados por fonte em `data/abertura-2026/` (default) vs. estender `SaldoInicialAno` com conta-corrente.
3. **De/para poder/órgão**: derivar de `Entidade.tipo`+`Orgao[]` com tabelinha de override (default) vs. mapa de config explícito.

Relaciona: [[oxy-playbook-verificacao-narrativa]] (ideia /memoriais/consistencia com selo), [[mdf-rgf-tesouro-referencia]], [[oxy-dashboards-integracao]], [[tce-pr-pit-dados-abertos]].
