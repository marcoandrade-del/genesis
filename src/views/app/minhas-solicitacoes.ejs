<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Minhas solicitações — Gênesis</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
  <%- include('../layouts/_theme') %>
</head>
<body class="bg-light">

<nav class="navbar navbar-dark bg-dark px-3 py-0" style="height:56px">
  <a class="navbar-brand fw-bold fs-6">
    <i class="bi bi-heptagon-fill text-primary me-2"></i>Gênesis
  </a>
  <a href="/app/logout" class="btn btn-sm btn-outline-secondary text-white border-secondary">
    <i class="bi bi-box-arrow-right me-1"></i>Sair
  </a>
</nav>

<div class="container py-5" style="max-width:820px">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="h4 fw-bold mb-0">Minhas solicitações de acesso</h2>
    <a href="/app/solicitar-acesso" class="btn btn-sm btn-primary">
      <i class="bi bi-plus-lg me-1"></i>Nova solicitação
    </a>
  </div>

  <% if (lista.length === 0) { %>
    <div class="alert alert-secondary small">
      Você ainda não fez nenhuma solicitação.
      <a href="/app/solicitar-acesso">Solicitar acesso a uma entidade</a>.
    </div>
  <% } else { %>
    <div class="card border-0 shadow-sm">
      <div class="table-responsive">
        <table class="table table-sm align-middle mb-0">
          <thead class="table-light">
            <tr>
              <th class="small">Entidade</th>
              <th class="small">Nível pedido</th>
              <th class="small">Status</th>
              <th class="small">Decisão</th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% lista.forEach(function(s) { %>
              <tr>
                <td>
                  <div class="fw-semibold small"><%= s.entidade.nome %></div>
                  <div class="text-muted" style="font-size:.75rem">
                    <%= s.entidade.municipio.estado.sigla %> · <%= s.entidade.municipio.nome %>
                  </div>
                </td>
                <td class="small"><%= s.nivelSolicitado %></td>
                <td>
                  <% if (s.status === 'PENDENTE') { %>
                    <span class="badge bg-warning bg-opacity-10 text-warning">Pendente</span>
                  <% } else if (s.status === 'APROVADA') { %>
                    <span class="badge bg-success bg-opacity-10 text-success">Aprovada</span>
                  <% } else if (s.status === 'REJEITADA') { %>
                    <span class="badge bg-danger bg-opacity-10 text-danger">Rejeitada</span>
                  <% } else { %>
                    <span class="badge bg-secondary bg-opacity-10 text-secondary">Cancelada</span>
                  <% } %>
                </td>
                <td class="small">
                  <% if (s.status === 'APROVADA') { %>
                    Nível <strong><%= s.nivelConcedido %></strong>
                  <% } %>
                  <% if (s.observacaoDecisao) { %>
                    <div class="text-muted" style="font-size:.75rem"><%= s.observacaoDecisao %></div>
                  <% } %>
                </td>
                <td class="text-end">
                  <% if (s.status === 'PENDENTE') { %>
                    <form method="POST" action="/app/minhas-solicitacoes/<%= s.id %>/cancelar"
                      onsubmit="return confirm('Cancelar esta solicitação?')">
                      <button class="btn btn-sm btn-outline-danger" type="submit">Cancelar</button>
                    </form>
                  <% } %>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>
  <% } %>

  <div class="mt-4">
    <a href="/app/contexto" class="small text-decoration-none">
      <i class="bi bi-arrow-left me-1"></i>Voltar ao contexto
    </a>
  </div>
</div>
</body>
</html>
